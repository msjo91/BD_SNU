"""
Lambda function
A nameless, inline, single-use function
lambda parameter1, parameter2, ...: definition
"""
# Case 1
f1 = lambda x, y: x * y


# Above is same as (except for the name):
def f2(x, y):
    return x * y
